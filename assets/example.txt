// This is an example file to test the preprocessor.

#include <stdio.h>

/*
 * This is a multi-line
 * block comment that should be removed.
 */
int main(int argc, char* argv[]) {

    // Define two integer variables with extra spaces
    int   a = 10;
    int b   = 20;

    int result = a + b; // Add them up

    printf("The result is: %d\n", result);

    return 0; /* Exit success */
}
